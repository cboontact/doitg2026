# 📘 คู่มือการใช้งานระบบกิจการนักเรียนดอยหัวเสือ

เอกสารฉบับนี้เขียนให้ใช้งานได้จริงทั้งตอนอบรมทีมและตอนเปิดระบบใช้ในโรงเรียน โดยเน้นให้อ่านง่าย เห็นภาพรวมไว และมีโฟลการทำงานของแต่ละส่วนชัดเจน

## 🌐 1. ภาพรวมของระบบ

ระบบกิจการนักเรียนดอยหัวเสือเป็นเว็บแอปสำหรับงานดูแลนักเรียนในแต่ละวัน โดยรวมงานสำคัญไว้ในระบบเดียว ได้แก่

- ✅ เช็คชื่อหน้าเสาธง
- ⏰ บันทึกมาสาย
- 🚶 ขอออกนอกบริเวณและสแกนออก-กลับ
- ⭐ จัดการคะแนนความประพฤติ
- 👩‍🏫 จัดการครูที่ปรึกษา
- 👤 จัดการผู้ใช้งาน
- 🧑‍🎓 พอร์ทัลนักเรียน
- 📝 ใบลาของนักเรียน
- 🎓 เลื่อนชั้นนักเรียน
- ⚙️ ตั้งค่าปีการศึกษาและวันหยุด

ระบบนี้ทำงานบน Cloudflare เป็นหลัก โดยใช้ Cloudflare Workers สำหรับ runtime และ Cloudflare D1 สำหรับฐานข้อมูล

## 🎯 2. เป้าหมายของระบบ

ระบบนี้ถูกออกแบบมาเพื่อแก้ปัญหา 4 เรื่องหลัก

1. ลดงานกระดาษและการจดซ้ำ
2. ให้ครูและเจ้าหน้าที่เห็นข้อมูลนักเรียนแบบทันที
3. ให้ข้อมูลการมาเรียน มาสาย ลา และพฤติกรรมเชื่อมกัน
4. เปิดทางให้นักเรียนตรวจสอบข้อมูลของตนเองได้

## 👥 3. บทบาทผู้ใช้งาน

ระบบรองรับ 4 บทบาทหลัก

### 👑 3.1 ผู้ดูแลระบบ `admin`

สิทธิ์หลัก:

- จัดการผู้ใช้งาน
- จัดการครูที่ปรึกษา
- จัดการนักเรียน
- ตั้งค่าระบบ
- เลื่อนชั้น
- ดูและแก้ไขข้อมูลได้ทุกส่วน

### 👩‍🏫 3.2 ครู `teacher`

สิทธิ์หลัก:

- เช็คชื่อหน้าเสาธง
- ดูข้อมูลนักเรียนในความดูแล
- ลงทะเบียนออกนอกบริเวณ
- ดูและแก้ไขคะแนนความประพฤติในขอบเขตที่ได้รับสิทธิ์
- เห็นใบลาของนักเรียนในหน้าเช็คชื่อ

หมายเหตุ:

- ครูที่ปรึกษาจะเห็นห้องของตนเองขึ้นก่อนในหลายหน้า
- ถ้าครูมีสิทธิ์ไม่ครบทุกห้อง ระบบจะจำกัดข้อมูลตามสิทธิ์

### 🧰 3.3 เจ้าหน้าที่ `staff`

สิทธิ์หลัก:

- บันทึกมาสาย
- เช็คชื่อหน้าเสาธง
- จัดการออกนอกบริเวณ
- ใช้งานจุดสแกน
- ดูข้อมูลนักเรียน
- ดูข้อมูลครูที่ปรึกษา
- จัดการคะแนนความประพฤติ

### 🧑‍🎓 3.4 นักเรียน `student`

สิทธิ์หลัก:

- ดูคะแนนความประพฤติของตัวเอง
- ดูประวัติการถูกเพิ่มและหักคะแนน
- ยื่นใบลา
- ดูสถานะใบลาของตัวเอง

## 🗺️ 4. หน้าใช้งานหลักของระบบ

### 🏠 4.1 หน้าหลักของระบบ

- `/login` หน้าเข้าสู่ระบบ
- `/dashboard` หน้าหลักสำหรับผู้ดูแล ครู และเจ้าหน้าที่
- `/student` หน้าหลักของนักเรียน

### 🛠️ 4.2 หน้าภายในแดชบอร์ด

- `/dashboard/attendance` เช็คชื่อหน้าเสาธง
- `/dashboard/late-checkin` บันทึกมาสาย
- `/dashboard/leave-requests` ขอออกนอกบริเวณ
- `/dashboard/leave-checkpoint` จุดสแกนออก-กลับ
- `/dashboard/behavior` คะแนนความประพฤติ
- `/dashboard/students` รายชื่อนักเรียน
- `/dashboard/teachers` ครูที่ปรึกษา
- `/dashboard/teacher-registrations` ลงทะเบียนครู
- `/dashboard/users` ผู้ใช้งาน
- `/dashboard/promotions` เลื่อนชั้น
- `/dashboard/settings` ตั้งค่าระบบ

### 🌍 4.3 หน้าสาธารณะ

- `/late-checkin` จุดบันทึกมาสาย
- `/leave-checkpoint` จุดสแกนออก-กลับ
- `/register/teacher` หน้าสมัครครู

หน้าสาธารณะเหมาะสำหรับเปิดค้างไว้ที่จุดใช้งานจริง เช่น จุดสแกนหน้าประตูหรือจุดออกนอกบริเวณ

## 🔐 5. การเข้าสู่ระบบ

### 👨‍💼 5.1 ผู้ดูแล ครู และเจ้าหน้าที่

1. เข้า `/login`
2. กรอกชื่อผู้ใช้
3. กรอกรหัสผ่าน
4. กด `เข้าสู่ระบบ`
5. ระบบจะพาไป `/dashboard`

### 🧑‍🎓 5.2 นักเรียน

1. เข้า `/login`
2. กรอก `ชื่อผู้ใช้ = เลขประจำตัวนักเรียน`
3. กรอก `รหัสผ่าน = เลขประจำตัวนักเรียน`
4. กด `เข้าสู่ระบบ`
5. ระบบจะพาไป `/student`

หมายเหตุ:

- นักเรียน login จากข้อมูลใน `students` โดยตรง ไม่ต้องมีบัญชีแยกในเมนู `ผู้ใช้งาน`
- ถ้าไม่มีการใช้งานเกิน 30 นาที ระบบจะให้ออกจากระบบและ login ใหม่

## 🧱 6. โครงสร้างสถาปัตยกรรมระบบ

ส่วนนี้อธิบายว่า “ระบบแบ่งชั้นการทำงานอย่างไร”

### 🏗️ 6.1 โครงสร้างระดับสูง

```text
+------------------+     +--------------------+     +----------------------+
|   ผู้ใช้งาน      | --> |   หน้าเว็บ Next.js | --> |   API ภายในระบบ      |
+------------------+     +--------------------+     |   (/api/*)           |
                                                     +----------------------+
                                                                |
                                                                v
                                                     +----------------------+
                                                     | business logic       |
                                                     | ใน src/lib           |
                                                     +----------------------+
                                                                |
                                                                v
                                                     +----------------------+
                                                     |  Cloudflare D1       |
                                                     |  ฐานข้อมูลหลัก       |
                                                     +----------------------+
```

### 🔩 6.2 ส่วนประกอบหลัก

- `Frontend`
  ใช้ Next.js App Router แสดงหน้า dashboard, หน้า login, หน้า public และพอร์ทัลนักเรียน

- `Backend API`
  ใช้ Route Handlers ใน `src/app/api` เป็น API ภายใน เช่น auth, attendance, behavior, leave requests และ students

- `Authentication`
  ใช้ cookie session และ middleware สำหรับควบคุมสิทธิ์การเข้าถึงหน้าและ API

- `Business Logic`
  เก็บใน `src/lib` เช่น กฎการหักคะแนน การตรวจสิทธิ์ การคำนวณสถานะ และการเชื่อมข้อมูลหลายตาราง

- `Database`
  ใช้ Cloudflare D1 เป็นฐานข้อมูลหลักของระบบ

### 🧩 6.3 โมดูลหลักของระบบ

- `โมดูลยืนยันตัวตน`
  login, logout, เปลี่ยนรหัสผ่าน, session timeout

- `โมดูลข้อมูลหลัก`
  users, teachers, students, academic years, holidays

- `โมดูลงานประจำวัน`
  attendance, late check-in, leave checkpoint

- `โมดูลพฤติกรรม`
  behavior score, behavior logs, score rules

- `โมดูลนักเรียน`
  student portal, student leave requests, ดูคะแนนของตัวเอง

- `โมดูลลงทะเบียนครู`
  teacher directory, teacher registration requests, approve / reset, import CSV

### 🗄️ 6.4 ตารางฐานข้อมูลหลัก

- `users` บัญชีผู้ใช้ของระบบ
- `teachers` ข้อมูลครูที่ปรึกษาและสิทธิ์ที่เกี่ยวข้อง
- `teacher_directory` รายชื่อครูที่เปิดให้ลงทะเบียน
- `teacher_registration_requests` คำขอลงทะเบียนครูที่รอแอดมินอนุมัติ
- `students` ข้อมูลนักเรียนหลัก
- `attendance` การเช็คชื่อประจำวัน
- `late_checkins` รายการมาสาย
- `leave_requests` คำขอออกนอกบริเวณ
- `behavior_logs` ประวัติการเพิ่มและหักคะแนน
- `score_rules` กฎคะแนน
- `academic_years` ปีการศึกษา
- `special_holidays` วันหยุดพิเศษ
- `student_leave_requests` ใบลาของนักเรียน

### 📚 6.5 Data Dictionary

ส่วนนี้อธิบาย field สำคัญของแต่ละตารางแบบย่อ เพื่อใช้คุยงาน อ้างอิงตอนติดตั้ง และช่วยให้เข้าใจความหมายของข้อมูลได้เร็ว

#### 👤 ตาราง `users`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสหลักของผู้ใช้ |
| `username` | TEXT | ชื่อผู้ใช้สำหรับ login |
| `passwordHash` | TEXT | รหัสผ่านที่ถูกเข้ารหัสแล้ว |
| `roles` | TEXT | รายการบทบาทในรูปแบบ JSON เช่น admin, teacher, staff |
| `firstName` | TEXT | ชื่อ |
| `lastName` | TEXT | นามสกุล |
| `teacherId` | TEXT | อ้างถึงครู ถ้าบัญชีนี้เป็นครู |
| `staffId` | TEXT | อ้างถึงเจ้าหน้าที่ ถ้ามี |
| `isActive` | INTEGER | สถานะเปิดใช้งานบัญชี |
| `mustChangePassword` | INTEGER | บังคับเปลี่ยนรหัสผ่านหลัง login หรือไม่ |
| `createdAt` | TEXT | วันที่สร้างบัญชี |

#### 👩‍🏫 ตาราง `teachers`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสหลักของข้อมูลครู |
| `userId` | TEXT | อ้างถึงบัญชีใน `users` |
| `firstName` | TEXT | ชื่อครู |
| `lastName` | TEXT | นามสกุลครู |
| `grade` | INTEGER | ระดับชั้นที่ดูแล |
| `room` | INTEGER | ห้องที่ดูแล |
| `isHeadOfLevel` | INTEGER | เป็นหัวหน้าระดับหรือไม่ |
| `isDisciplineTeacher` | INTEGER | เป็นครูฝ่ายปกครองหรือไม่ |
| `levelScope` | INTEGER | ขอบเขตระดับชั้นที่รับผิดชอบ |
| `phone` | TEXT | เบอร์ติดต่อ |
| `createdAt` | TEXT | วันที่สร้างข้อมูล |

#### 🗂️ ตาราง `teacher_directory`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสหลักของรายชื่อครู |
| `prefix` | TEXT | คำนำหน้าเดิม ถ้ามี |
| `firstName` | TEXT | ใช้เก็บชื่อเต็มของครูในระบบปัจจุบัน |
| `lastName` | TEXT | เว้นว่างได้ ถ้าใช้ชื่อเต็มช่องเดียว |
| `phone` | TEXT | ช่องสำรองเดิมของรายชื่อครู |
| `isActive` | INTEGER | เปิดให้ลงทะเบียนหรือไม่ |
| `isRegistered` | INTEGER | ครูคนนี้ถูกอนุมัติและสร้างบัญชีแล้วหรือยัง |
| `currentUserId` | TEXT | อ้างถึงบัญชีผู้ใช้ที่สร้างจากคำขอนี้ |
| `currentTeacherId` | TEXT | อ้างถึงข้อมูลครูในตาราง `teachers` |
| `createdAt` | TEXT | วันที่เพิ่มรายชื่อ |
| `updatedAt` | TEXT | วันที่แก้ไขล่าสุด |

#### 📨 ตาราง `teacher_registration_requests`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสคำขอลงทะเบียน |
| `teacherDirectoryId` | TEXT | อ้างถึงรายชื่อครูที่เปิดให้ลงทะเบียน |
| `username` | TEXT | ชื่อผู้ใช้ที่ครูตั้งเอง |
| `passwordHash` | TEXT | รหัสผ่านที่เข้ารหัสแล้ว |
| `phone` | TEXT | เบอร์โทรที่ครูกรอกเองตอนสมัคร |
| `grade` | INTEGER | ชั้นที่ครูระบุว่าดูแล |
| `room` | INTEGER | ห้องที่ครูระบุว่าดูแล |
| `status` | TEXT | สถานะคำขอ เช่น pending, approved, rejected, reset |
| `submittedAt` | TEXT | วันที่ส่งคำขอ |
| `reviewedAt` | TEXT | วันที่แอดมินตรวจคำขอ |
| `reviewedBy` | TEXT | ผู้ตรวจคำขอ |
| `reviewedByName` | TEXT | ชื่อผู้ตรวจคำขอ |
| `reviewNote` | TEXT | หมายเหตุจากการพิจารณา |
| `createdUserId` | TEXT | บัญชีผู้ใช้ที่ถูกสร้างตอนอนุมัติ |
| `createdTeacherId` | TEXT | ข้อมูลครูที่ถูกสร้างตอนอนุมัติ |

#### 🧑‍🎓 ตาราง `students`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสหลักของนักเรียน |
| `studentId` | TEXT | เลขประจำตัวนักเรียน |
| `studentCardId` | TEXT | รหัสบัตร RFID |
| `prefix` | TEXT | คำนำหน้า |
| `firstName` | TEXT | ชื่อ |
| `lastName` | TEXT | นามสกุล |
| `grade` | INTEGER | ระดับชั้น |
| `room` | INTEGER | ห้อง |
| `behaviorScore` | INTEGER | คะแนนความประพฤติคงเหลือ |
| `isActive` | INTEGER | สถานะกำลังใช้งานในระบบ |
| `studyStatus` | TEXT | สถานะการเรียน เช่น studying, graduated, withdrawn |
| `lastPromotionAcademicYearId` | TEXT | ปีการศึกษาที่เลื่อนชั้นล่าสุด |
| `graduatedAt` | TEXT | วันที่จบการศึกษา |
| `graduationAcademicYearId` | TEXT | ปีการศึกษาที่จบ |
| `createdAt` | TEXT | วันที่สร้างข้อมูล |

#### ✅ ตาราง `attendance`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสรายการเช็คชื่อ |
| `date` | TEXT | วันที่เช็คชื่อ |
| `studentId` | TEXT | อ้างถึงนักเรียน |
| `grade` | INTEGER | ชั้นของนักเรียน ณ วันที่บันทึก |
| `room` | INTEGER | ห้องของนักเรียน ณ วันที่บันทึก |
| `status` | TEXT | สถานะ เช่น present, absent, late, leave_sick, leave_personal |
| `recordedBy` | TEXT | ผู้บันทึก |
| `recordedAt` | TEXT | วันเวลาในการบันทึก |
| `source` | TEXT | ที่มาของข้อมูล เช่น manual หรือ late_scan |
| `lockedByLate` | INTEGER | ถูกล็อกจากระบบมาสายหรือไม่ |
| `lateCheckInId` | TEXT | อ้างถึงรายการมาสายที่เกี่ยวข้อง |

#### ⏰ ตาราง `late_checkins`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสรายการมาสาย |
| `date` | TEXT | วันที่มาสาย |
| `studentId` | TEXT | อ้างถึงนักเรียน |
| `grade` | INTEGER | ชั้นของนักเรียน |
| `room` | INTEGER | ห้องของนักเรียน |
| `scanValue` | TEXT | ค่าที่ใช้สแกน เช่น RFID หรือเลขประจำตัว |
| `matchedBy` | TEXT | วิธีที่ระบบจับคู่ เช่น student_id หรือ rfid |
| `channel` | TEXT | มาจากจุดสแกน public หรือ internal |
| `recordedBy` | TEXT | ผู้บันทึก |
| `recordedByName` | TEXT | ชื่อผู้บันทึก |
| `recordedAt` | TEXT | วันเวลาที่บันทึก |
| `behaviorDeducted` | INTEGER | หักคะแนนแล้วหรือยัง |

#### 🚶 ตาราง `leave_requests`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสคำขอออกนอกบริเวณ |
| `date` | TEXT | วันที่ทำรายการ |
| `studentId` | TEXT | อ้างถึงนักเรียน |
| `grade` | INTEGER | ชั้นของนักเรียน |
| `room` | INTEGER | ห้องของนักเรียน |
| `requestedBy` | TEXT | ผู้สร้างคำขอ |
| `requestedByName` | TEXT | ชื่อผู้สร้างคำขอ |
| `plannedDepartureAt` | TEXT | เวลาออกที่วางแผนไว้ |
| `plannedReturnAt` | TEXT | เวลากลับที่วางแผนไว้ |
| `noReturn` | INTEGER | ไม่กลับเข้าโรงเรียนหรือไม่ |
| `destination` | TEXT | สถานที่ปลายทาง |
| `travelMode` | TEXT | รูปแบบการเดินทาง |
| `contactPhone` | TEXT | เบอร์ติดต่อ |
| `note` | TEXT | หมายเหตุ |
| `status` | TEXT | สถานะคำขอ เช่น pending_departure, departed, returned_late |
| `actualDepartureAt` | TEXT | เวลาออกจริง |
| `actualReturnAt` | TEXT | เวลากลับจริง |
| `lateDeducted` | INTEGER | หักคะแนนกรณีกลับช้าแล้วหรือยัง |
| `missingReturnDeducted` | INTEGER | หักคะแนนกรณีไม่กลับแล้วหรือยัง |
| `createdAt` | TEXT | วันที่สร้างรายการ |

#### ⭐ ตาราง `behavior_logs`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสรายการพฤติกรรม |
| `studentId` | TEXT | อ้างถึงนักเรียน |
| `studentName` | TEXT | ชื่อนักเรียน ณ เวลาบันทึก |
| `scoreBefore` | INTEGER | คะแนนก่อนเปลี่ยน |
| `scoreChange` | INTEGER | คะแนนที่เพิ่มหรือหัก |
| `scoreAfter` | INTEGER | คะแนนหลังเปลี่ยน |
| `type` | TEXT | ประเภท add หรือ deduct |
| `reason` | TEXT | เหตุผล |
| `ruleId` | TEXT | อ้างถึงกฎคะแนนถ้ามี |
| `performedBy` | TEXT | ผู้ดำเนินการ |
| `performedByName` | TEXT | ชื่อผู้ดำเนินการ |
| `createdAt` | TEXT | วันเวลาที่บันทึก |

#### 📏 ตาราง `score_rules`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสกฎคะแนน |
| `name` | TEXT | ชื่อกฎ |
| `type` | TEXT | ประเภท add หรือ deduct |
| `points` | INTEGER | จำนวนคะแนน |
| `category` | TEXT | หมวดหมู่ |
| `isActive` | INTEGER | เปิดใช้งานหรือไม่ |
| `createdAt` | TEXT | วันที่สร้างกฎ |

#### 📅 ตาราง `academic_years`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสปีการศึกษา |
| `name` | TEXT | ชื่อปีการศึกษา |
| `term1Start` | TEXT | วันเปิดภาคเรียนที่ 1 |
| `term1End` | TEXT | วันปิดภาคเรียนที่ 1 |
| `term2Start` | TEXT | วันเปิดภาคเรียนที่ 2 |
| `term2End` | TEXT | วันปิดภาคเรียนที่ 2 |
| `isActive` | INTEGER | เป็นปีการศึกษาปัจจุบันหรือไม่ |
| `createdAt` | TEXT | วันที่สร้างข้อมูล |

#### 🎉 ตาราง `special_holidays`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสวันหยุด |
| `date` | TEXT | วันที่หยุด |
| `name` | TEXT | ชื่อวันหยุด |
| `isActive` | INTEGER | ใช้งานอยู่หรือไม่ |
| `createdAt` | TEXT | วันที่สร้างข้อมูล |

#### 📝 ตาราง `student_leave_requests`

| ฟิลด์ | ชนิดข้อมูล | ความหมาย |
|---|---|---|
| `id` | TEXT | รหัสใบลา |
| `studentId` | TEXT | อ้างถึงนักเรียน |
| `grade` | INTEGER | ชั้นของนักเรียน |
| `room` | INTEGER | ห้องของนักเรียน |
| `date` | TEXT | วันที่ลา |
| `leaveType` | TEXT | ประเภทลา เช่น sick หรือ personal |
| `reason` | TEXT | เหตุผลการลา |
| `note` | TEXT | หมายเหตุเพิ่มเติม |
| `status` | TEXT | สถานะใบลา เช่น pending, recorded, cancelled |
| `createdAt` | TEXT | วันที่สร้างใบลา |
| `reviewedAt` | TEXT | วันที่ถูกนำไปใช้งานหรือถูกทบทวน |
| `reviewedBy` | TEXT | ผู้ที่ดำเนินการกับใบลา |
| `reviewedByName` | TEXT | ชื่อผู้ที่ดำเนินการ |
| `attendanceRecordId` | TEXT | อ้างถึง attendance ที่ใช้ใบลานี้ |

### 📁 6.6 โครงสร้างโค้ดหลัก

```text
src/
  app/
    api/                  # API ภายในระบบ
    dashboard/            # หน้าสำหรับ admin / teacher / staff
    student/              # พอร์ทัลนักเรียน
    late-checkin/         # จุดบันทึกมาสาย
    leave-checkpoint/     # จุดสแกนออก-กลับ
    login/                # หน้าเข้าสู่ระบบ
  components/             # ส่วนประกอบ UI
  lib/                    # auth, db, business logic
docs/
  user-guide.md           # คู่มือฉบับนี้
schema.sql                # โครงสร้างฐานข้อมูลสำหรับติดตั้งใหม่
```

## 🔄 7. โฟลการทำงานของระบบ

ส่วนนี้อธิบาย “ใครกดอะไร แล้วข้อมูลไหลไปไหน”

### 🔐 7.1 โฟลการ login

```text
+------------------------+
| ผู้ใช้กรอกข้อมูล       |
+------------------------+
            |
            v
+------------------------+
| หน้า /login            |
+------------------------+
            |
            v
+------------------------+
| /api/auth/login        |
+------------------------+
      |             |
      v             v
+-----------+        +-------------+
|  users    |        |  students   |
+-----------+        +-------------+
      |                     |
      |                     |
      |             login ด้วยเลขประจำตัว
      |                     |
      +----------+----------+
                 |
                 v
      +----------------------+
      | สร้าง session cookie |
      +----------------------+
                 |
                 v
      +----------------------+
      | ตรวจบทบาทผู้ใช้      |
      +----------------------+
           |             |
           v             v
    +-------------+   +-----------+
    | /dashboard  |   | /student  |
    +-------------+   +-----------+
```

ขั้นตอน:

1. ผู้ใช้กรอกชื่อผู้ใช้และรหัสผ่าน
2. หน้า login ส่งข้อมูลไป `/api/auth/login`
3. ถ้าเป็นนักเรียน ระบบตรวจใน `students` โดยตรง
4. ถ้าเป็นบุคลากร ระบบตรวจบัญชีใน `users`
5. ระบบสร้าง token และ cookie
6. พาไปหน้าตามบทบาทของผู้ใช้

### 👩‍🏫 7.1.1 โฟลลงทะเบียนครู

```text
+------------------------+
| ครูเปิดหน้าลงทะเบียน   |
+------------------------+
            |
            v
+------------------------------+
| เลือกชื่อจาก teacher_directory |
+------------------------------+
            |
            v
+------------------------+
| กรอก username/password |
| เบอร์โทร ชั้น ห้อง      |
+------------------------+
            |
            v
+------------------------------+
| บันทึกลง teacher_registration |
| status = pending             |
+------------------------------+
            |
            v
+------------------------+
| แอดมินเปิดคำขอ         |
+------------------------+
            |
     +------+------+
     |             |
     v             v
+-----------+  +------------------+
| อนุมัติ   |  | ไม่อนุมัติ/ยกเลิก |
+-----------+  +------------------+
     |                    |
     v                    v
+---------------------------+   +----------------------+
| สร้าง users/teachers      |   | เปิดให้สมัครใหม่ได้    |
| และล็อกชื่อในรายการสมัคร |   +----------------------+
+---------------------------+
```

ขั้นตอน:

1. แอดมินต้องเพิ่มรายชื่อครูก่อนในแท็บ `เพิ่มรายชื่อครู`
2. ครูเปิดหน้าสมัครและเลือกได้เฉพาะชื่อที่ยัง active และยังไม่ถูกอนุมัติ
3. ครูกรอก `username`, `password`, `เบอร์โทร`, `ชั้น`, `ห้อง`
4. ระบบเก็บคำขอไว้ใน `teacher_registration_requests`
5. แอดมินเปิดแท็บ `คำขอลงทะเบียน` เพื่อตรวจคำขอ
6. ถ้าอนุมัติ ระบบจะสร้างข้อมูลใน `users` และ `teachers` อัตโนมัติ
7. ถ้าคำขอถูกอนุมัติแล้ว จะไม่รีเซ็ตจากหน้านี้อีก ต้องไปแก้ต่อที่เมนู `ผู้ใช้งาน` หรือ `ครูที่ปรึกษา`
8. ถ้าคำขอยังไม่อนุมัติ แอดมินยังสามารถไม่อนุมัติหรือยกเลิกได้

### ✅ 7.2 โฟลเช็คชื่อหน้าเสาธง

```text
+------------------------+
| ครูหรือเจ้าหน้าที่      |
+------------------------+
            |
            v
+------------------------+
| เลือกชั้นและห้อง       |
+------------------------+
            |
            v
+------------------------+
| โหลดรายชื่อนักเรียน     |
+------------------------+
            |
            v
+------------------------+
| โหลดใบลาของนักเรียน    |
+------------------------+
            |
            v
+------------------------+
| เลือกสถานะของแต่ละคน    |
+------------------------+
            |
            v
+------------------------+
| บันทึกการเช็คชื่อ       |
+------------------------+
      |             |
      v             v
+-------------+  +----------------------+
| attendance  |  | student_leave_req.  |
+-------------+  +----------------------+
      |
      v
+------------------------+
| อัปเดตสถิติห้อง        |
+------------------------+
```

ขั้นตอน:

1. ครูหรือเจ้าหน้าที่เปิดหน้าเช็คชื่อ
2. เลือกชั้นและห้อง
3. ระบบโหลดนักเรียนของห้องนั้น
4. ระบบโหลดใบลาของนักเรียนในวันเดียวกันมาประกอบ
5. ผู้ใช้เลือกสถานะ เช่น มา ขาด มาสาย ลาป่วย ลากิจ
6. กดบันทึก
7. API บันทึกลง `attendance`
8. ถ้ามีใบลาที่ตรงกัน ระบบจะ mark ว่าใบลานั้นถูกใช้แล้ว
9. หน้าเลื่อนกลับขึ้นไปให้เห็นสถิติของห้อง

### ⏰ 7.3 โฟลบันทึกมาสาย

```text
+------------------------------+
| สแกนบัตรหรือกรอกเลขประจำตัว |
+------------------------------+
               |
               v
+------------------------+
| ค้นหานักเรียน          |
+------------------------+
               |
               v
+------------------------+
| students               |
+------------------------+
               |
               v
+------------------------+
| บันทึก late_checkins   |
+------------------------+
      |             |
      v             v
+---------------+ +--------------+
| late_checkins | | attendance   |
+---------------+ +--------------+
                     |
                     v
         +----------------------------+
         | เข้าเงื่อนไขหักคะแนนไหม   |
         +----------------------------+
                     |
                  ใช่ v
              +---------------+
              | behavior_logs |
              +---------------+
```

ขั้นตอน:

1. เจ้าหน้าที่เปิดหน้า `/late-checkin` หรือหน้าใน dashboard
2. สแกน RFID หรือกรอกเลขประจำตัว
3. ระบบค้นหานักเรียนใน `students`
4. ถ้าบัตรยังไม่ถูกผูก ระบบจะให้ผูกก่อน
5. บันทึกรายการลง `late_checkins`
6. ปรับสถานะการเช็คชื่อของวันนั้นเป็น `late`
7. ถ้าเข้าเงื่อนไข ระบบจะบันทึกการหักคะแนนลง `behavior_logs`

### 🚶 7.4 โฟลขอออกนอกบริเวณ

```text
+------------------------+
| ครูหรือเจ้าหน้าที่      |
+------------------------+
            |
            v
+------------------------+
| ค้นหานักเรียน          |
+------------------------+
            |
            v
+------------------------+
| กรอกเวลาและรายละเอียด   |
+------------------------+
            |
            v
+------------------------+
| บันทึกคำขอ             |
+------------------------+
            |
            v
+------------------------+
| leave_requests         |
+------------------------+
            |
            v
+------------------------+
| สแกนออก               |
+------------------------+
            |
            v
+------------------------+
| สแกนกลับ              |
+------------------------+
            |
            v
+------------------------------+
| ประเมินกลับตรงเวลา/ช้า/ไม่กลับ |
+------------------------------+
            |
            v
+------------------------+
| behavior_logs          |
+------------------------+
```

ขั้นตอน:

1. ครูหรือเจ้าหน้าที่ค้นหานักเรียน
2. กรอกเวลาออก เวลากลับ ปลายทาง การเดินทาง และเบอร์ติดต่อ
3. ระบบตรวจว่าข้อมูลเวลาใช้ได้หรือไม่
4. ระบบตรวจว่านักเรียนมีคำขอค้างอยู่หรือไม่
5. ถ้าเป็นครูที่ปรึกษาส่งคำขอ ระบบจะบันทึกเป็น `รอยืนยัน`
6. เจ้าหน้าที่หรือผู้ดูแลระบบกด `ยืนยันรายการ` แล้วคำขอจะเปลี่ยนเป็น `รอออก`
7. ถ้าเจ้าหน้าที่หรือผู้ดูแลระบบเป็นคนส่งคำขอเอง ระบบจะพร้อมสแกนทันที
8. เมื่อนักเรียนไปถึงจุดสแกน ระบบจะบันทึกเวลาออกจริง
9. ตอนนักเรียนกลับ ระบบจะบันทึกเวลากลับจริง
10. ถ้ากลับช้าหรือไม่กลับ ระบบอาจหักคะแนนและบันทึกลง `behavior_logs`

### 📝 7.5 โฟลใบลาของนักเรียน

```text
+------------------------+
| นักเรียน               |
+------------------------+
            |
            v
+------------------------+
| ยื่นใบลา               |
+------------------------+
            |
            v
+------------------------+
| student_leave_requests |
+------------------------+
            |
            v
+------------------------+
| ครูเปิดหน้าเช็คชื่อ    |
+------------------------+
            |
            v
+------------------------+
| ระบบแสดงคำแนะนำตามใบลา |
+------------------------+
            |
            v
+------------------------+
| ครูเลือกสถานะลา        |
+------------------------+
            |
            v
+------------------------+
| attendance             |
+------------------------+
```

ขั้นตอน:

1. นักเรียน login เข้าพอร์ทัลนักเรียน
2. เข้าเมนู `ใบลาของฉัน`
3. เลือกประเภทใบลา เช่น ลาป่วย หรือ ลากิจ
4. กรอกเหตุผลและวันที่ลา
5. ระบบบันทึกลง `student_leave_requests`
6. ครูเปิดหน้าเช็คชื่อของวันนั้น
7. ระบบดึงใบลานั้นมาแสดงใต้ชื่อนักเรียน
8. ปุ่มสถานะที่ตรงกับใบลาจะแสดง `ตามใบลา`
9. ครูบันทึกเช็คชื่อ และระบบจะผูกใบลากับ attendance

### ⭐ 7.6 โฟลคะแนนความประพฤติ

```text
+------------------------+
| ครูหรือเจ้าหน้าที่      |
+------------------------+
            |
            v
+------------------------+
| เลือกนักเรียน          |
+------------------------+
            |
            v
+------------------------+
| ดูคะแนนปัจจุบัน         |
+------------------------+
            |
            v
+------------------------------+
| เลือกกฎคะแนนหรือกรอกเหตุผล |
+------------------------------+
            |
            v
+------------------------+
| คำนวณคะแนนก่อนและหลัง    |
+------------------------+
      |             |
      v             v
+---------------+ +------------------------+
| behavior_logs | | students.behaviorScore |
+---------------+ +------------------------+
```

ขั้นตอน:

1. ครูหรือเจ้าหน้าที่เปิดหน้า `คะแนนความประพฤติ`
2. เลือกชั้น ห้อง หรือค้นหานักเรียน
3. ระบบดึงคะแนนปัจจุบันและประวัติคะแนน
4. ผู้ใช้เลือกกฎคะแนนหรือกรอกเหตุผลเอง
5. ระบบคำนวณคะแนนก่อนและหลัง
6. บันทึกลง `behavior_logs`
7. อัปเดต `students.behaviorScore`

### 🧑‍🎓 7.7 โฟลนักเรียนดูคะแนนตัวเอง

```text
+-----------------------------+
| นักเรียนเปิด /student/behavior |
+-----------------------------+
               |
               v
+------------------------+
| ตรวจสิทธิ์ student     |
+------------------------+
               |
               v
+------------------------+
| เรียก API ข้อมูลพฤติกรรม |
+------------------------+
      |             |
      v             v
+-------------+  +---------------+
| students    |  | behavior_logs |
+-------------+  +---------------+
      |             |
      v             v
+-------------+  +---------------+
| แสดงคะแนน   |  | แสดงประวัติ  |
+-------------+  +---------------+
```

ขั้นตอน:

1. นักเรียน login
2. เข้า `/student/behavior`
3. ระบบตรวจ role ว่าเป็น `student`
4. API ดึงเฉพาะข้อมูลของนักเรียนคนนั้น
5. แสดงคะแนนคงเหลือและประวัติการเพิ่มหรือหักคะแนน

### 👩‍🏫 7.8 โฟลจัดการครูที่ปรึกษา

```text
+------------------------+
| ผู้ดูแลระบบ            |
+------------------------+
            |
            v
+------------------------+
| เลือกครู               |
+------------------------+
            |
            v
+------------------------+
| กำหนดชั้น ห้อง สิทธิ์  |
+------------------------+
            |
            v
+------------------------+
| teachers               |
+------------------------+
            |
            v
+------------------------------+
| ระบบใช้ข้อมูลนี้ไป filter |
| และเรียงข้อมูล             |
+------------------------------+
```

ขั้นตอน:

1. ผู้ดูแลเปิดหน้า `ครูที่ปรึกษา`
2. เลือกบัญชีครู
3. เลือกชั้นและห้อง
4. กำหนดสิทธิ์เพิ่มเติมถ้ามี
5. บันทึกลง `teachers`
6. ระบบใช้ข้อมูลนี้ในการเรียงและกรองข้อมูลนักเรียนในหน้าต่าง ๆ

### 🎓 7.9 โฟลเลื่อนชั้นนักเรียน

```text
+------------------------+
| ผู้ดูแลระบบ            |
+------------------------+
            |
            v
+------------------------+
| สร้างปีการศึกษาใหม่    |
+------------------------+
            |
            v
+------------------------+
| ตรวจข้อมูลนักเรียน      |
+------------------------+
            |
            v
+------------------------+
| ยืนยันการเลื่อนชั้น     |
+------------------------+
      |             |
      v             v
+-------------+  +----------------+
| students    |  | academic_years |
+-------------+  +----------------+
```

ขั้นตอน:

1. ผู้ดูแลสร้างปีการศึกษาปลายทาง
2. เปิดหน้า `เลื่อนชั้นนักเรียน`
3. ระบบสรุปจำนวนนักเรียนที่จะถูกเลื่อน
4. ผู้ดูแลตรวจสอบข้อมูล
5. ยืนยันการดำเนินการ
6. ระบบอัปเดตชั้น ห้อง หรือสถานะจบการศึกษา

## 👑 8. คู่มือสำหรับผู้ดูแลระบบ

### 🚀 8.1 สิ่งที่ควรตรวจหลัง login

1. วันที่ระบบถูกต้อง
2. ปีการศึกษาที่ active ถูกต้อง
3. การ์ดสรุปข้อมูลไม่ผิดปกติ
4. ไม่มีห้องหรือรายชื่อนักเรียนหาย

### 👤 8.2 เมนูผู้ใช้งาน

ใช้สำหรับ:

- เพิ่มผู้ใช้ใหม่
- แก้ไขข้อมูลผู้ใช้
- เปลี่ยนบทบาท
- รีเซ็ตรหัสผ่าน
- เปิดหรือปิดบัญชี

หมายเหตุ:

- เมนูนี้ใช้สำหรับ `admin`, `teacher`, `staff`
- ไม่ใช้สร้างบัญชีนักเรียน
- นักเรียนใช้เลขประจำตัวเป็นชื่อผู้ใช้และรหัสผ่านจากเมนู `รายชื่อนักเรียน`

### 👩‍🏫 8.3 เมนูลงทะเบียนครู

ใช้สำหรับ:

- ดูแท็บ `คำขอลงทะเบียน`
- ดูแท็บ `เพิ่มรายชื่อครู`
- เพิ่มรายชื่อทีละคน
- import รายชื่อด้วย CSV
- ดาวน์โหลดเท็มเพลต CSV
- อนุมัติ, ไม่อนุมัติ, หรือยกเลิกคำขอที่ยังไม่อนุมัติ
- ลบรายชื่อครูที่ยังไม่ถูกอนุมัติ
- ลบคำขอลงทะเบียนที่ยังไม่ถูกอนุมัติ

ลักษณะการใช้งาน:

- แท็บ `คำขอลงทะเบียน` ใช้ดูคำขอของครูที่ส่งเข้ามา
- แท็บ `เพิ่มรายชื่อครู` ใช้จัดการรายชื่อครูที่เปิดสิทธิ์ให้สมัคร
- ปุ่ม `ดาวน์โหลดเท็มเพลต`, `อัปโหลด CSV`, และ `เพิ่มรายชื่อครู` จะอยู่ในแท็บ `เพิ่มรายชื่อครู`
- ถ้าครูคนใด `อนุมัติแล้ว` รายชื่อนั้นจะแสดงเพียงสถานะ `อนุมัติแล้ว` และจะไม่แก้ไขจากเมนูนี้
- ถ้ารายชื่อครูยังไม่ถูกอนุมัติ สามารถลบออกจากรายการได้
- ถ้าคำขอลงทะเบียนยังไม่ถูกอนุมัติ สามารถลบคำขอได้
- หากต้องการเปลี่ยนข้อมูลหลังอนุมัติ ให้ไปแก้ที่เมนู `ผู้ใช้งาน` หรือ `ครูที่ปรึกษา`

### 👩‍🏫 8.4 เมนูครูที่ปรึกษา

ใช้สำหรับ:

- ผูกครูกับห้อง
- ระบุหัวหน้าระดับ
- ระบุขอบเขตการดูแล
- ให้เจ้าหน้าที่ดูข้อมูลได้แบบอ่านอย่างเดียว

### 🧑‍🎓 8.5 เมนูรายชื่อนักเรียน

ใช้สำหรับ:

- เพิ่มและแก้ไขนักเรียน
- import ข้อมูล
- ตรวจ RFID
- ตรวจคะแนนตั้งต้น
- ใช้เป็นแหล่งข้อมูล login ของนักเรียนโดยตรง
- เลือก `ระดับชั้น` และ `ห้อง` แล้วกดค้นหา
- ดูผลลัพธ์แบบแบ่งหน้า และเลือกจำนวนรายการต่อหน้า `30 / 40 / 50 / 100`

### ⚙️ 8.6 เมนูตั้งค่าระบบ

ใช้สำหรับ:

- ปีการศึกษา
- วันหยุดพิเศษ
- ช่วงเปิดภาคเรียน

### 🎓 8.7 เมนูเลื่อนชั้น

ใช้สำหรับ:

- ย้ายนักเรียนไปปีถัดไป
- จบชั้นสำหรับชั้นปลาย

## 👩‍🏫 9. คู่มือสำหรับครู

### 📝 9.0 การสมัครใช้งานครู

ลำดับการใช้งาน:

1. รับลิงก์หน้าสมัครจากแอดมิน
2. เลือกชื่อครูของตนเอง
3. กรอก `username`, `password`, `เบอร์โทร`, `ชั้น`, `ห้อง`
4. ส่งคำขอลงทะเบียน
5. รอแอดมินอนุมัติ
6. เมื่ออนุมัติแล้วจึง login เข้าระบบได้

### ✅ 9.1 เช็คชื่อหน้าเสาธง

ลำดับการใช้งาน:

1. เลือกห้อง
2. เช็กสถานะนักเรียน
3. ดูคำแนะนำจากใบลาถ้ามี
4. กดบันทึก

### 🚶 9.2 ขอออกนอกบริเวณ

ลำดับการใช้งาน:

1. ค้นหานักเรียน
2. กรอกข้อมูลเวลา
3. กรอกข้อมูลปลายทางและการติดต่อ
4. กดบันทึกคำขอ

### ⭐ 9.3 คะแนนความประพฤติ

ลำดับการใช้งาน:

1. เลือกชั้นและห้อง
2. ค้นหานักเรียน
3. ตรวจคะแนนเดิม
4. เพิ่มหรือหักคะแนน
5. บันทึก

## 🧰 10. คู่มือสำหรับเจ้าหน้าที่

### ⏰ 10.1 จุดบันทึกมาสาย

ลำดับการใช้งาน:

1. สแกน RFID หรือกรอกเลขประจำตัว
2. ตรวจสอบชื่อ
3. ยืนยันการบันทึก

### 🔁 10.2 จุดสแกนออก-กลับ

ลำดับการใช้งาน:

1. ตรวจว่ามีคำขอออกนอกบริเวณแล้ว
2. สแกนตอนออก
3. สแกนตอนกลับ
4. ตรวจสถานะล่าสุด

## 🧑‍🎓 11. คู่มือสำหรับนักเรียน

### 🏠 11.1 หน้าแรกนักเรียน

ใช้สำหรับดู:

- คะแนนคงเหลือ
- รายการล่าสุด
- สถานะใบลา

### ⭐ 11.2 ดูคะแนนความประพฤติ

ใช้สำหรับดู:

- คะแนนปัจจุบัน
- เหตุผลที่ถูกหัก
- เหตุผลที่ได้รับเพิ่ม

### 📝 11.3 ยื่นใบลา

ลำดับการใช้งาน:

1. เข้าเมนูใบลา
2. เลือกประเภทลา
3. กรอกเหตุผล
4. บันทึก

## 🧪 12. ปัญหาที่พบบ่อย

### ❌ 12.1 login ไม่ได้

ให้ตรวจ:

1. ชื่อผู้ใช้ถูกหรือไม่
2. รหัสผ่านถูกหรือไม่
3. ถ้าเป็นนักเรียน ใช้เลขประจำตัวทั้งสองช่องหรือไม่
4. บัญชียัง active หรือไม่

### 👀 12.2 ไม่เห็นรายชื่อนักเรียน

ให้ตรวจ:

1. เลือกห้องถูกหรือไม่
2. สิทธิ์ครูถูกตั้งหรือยัง
3. นักเรียนยัง active หรือไม่

### 📍 12.3 สแกนออกไม่ได้

ให้ตรวจ:

1. มีคำขอออกนอกบริเวณก่อนหรือยัง
2. สแกนบัตรถูกคนหรือไม่
3. คำขอเดิมยังค้างหรือไม่

## 💡 13. ข้อแนะนำก่อนใช้จริง

- ควรตั้งปีการศึกษาให้ถูกก่อนเปิดใช้งาน
- ควร import นักเรียนและตรวจห้องให้ครบ
- ควรผูกครูที่ปรึกษาให้ครบก่อนวันเปิดเทอม
- ควรทดสอบจุดสแกนทุกเช้า
- ควรให้นักเรียนทดลอง login ก่อนใช้จริง
- ควรสำรองข้อมูล D1 เป็นระยะ

## ✅ 14. สรุป

ถ้าจะเริ่มใช้ระบบนี้แบบเรียงลำดับที่ถูก ควรทำดังนี้

1. ตั้งค่าปีการศึกษาและวันหยุด
2. import นักเรียน
3. สร้างผู้ใช้ครูและเจ้าหน้าที่
4. ผูกครูที่ปรึกษากับห้อง
5. ทดสอบ login ทุกบทบาท
6. ทดสอบเช็คชื่อ มาสาย ออกนอกบริเวณ และพอร์ทัลนักเรียน

เมื่อครบ 6 ขั้นตอนนี้ ระบบก็พร้อมใช้งานจริงในโรงเรียน
